#include <stdio.h>
#include <stdlib.h>
  int n, sum = 0;
  int d = 1, i = 1;

  function()
    {
    	if(n%2 == 0)
    	  if((n%2)==0)
	{
	  i=1;	
	  while(i<n)
	  {
	  sum=sum+i;
	  i=i+2	  ;	
	  printf("input number is #");
	  printf("%d",sum);
	  }
	}
		if((n%2)!=0)
	i=1;	
	while(i<=n)
	{
	sum=sum+i;
	i=i+2;
	printf("input number is #");
	printf("%d",sum);
    }
	}
  
  int main(void)
    {
    	printf("Enter positive number : ");
    	scanf("%d",&n);
    	function();
	}
